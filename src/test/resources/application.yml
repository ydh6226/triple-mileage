spring:
  jpa:
    hibernate:
      ddl-auto: create

  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:testdb
      username: sa

  flyway:
    enabled: false

decorator:
  datasource:
    p6spy:
      enable-logging: false
